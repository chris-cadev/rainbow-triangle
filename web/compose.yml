name: rainbow-triangle-web
services:
  server:
    build:
      context: ../
      dockerfile: web/Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped

  tunnel:
    image: cloudflare/cloudflared:latest
    command: tunnel --url http://server:80
    restart: unless-stopped
    depends_on:
      - server
